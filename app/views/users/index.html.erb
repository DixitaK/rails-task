<h2 class="link">Users List</h2>
<table>
  <thead>
    <th>Name</th>
    <th>Contact</th>
    <th>Email</th>
  </thead>
  <tbody>

    <% @users.each do |user| %>
    <tr>
        <td><%= user.name %></td>
        <td><%= user.phone %></td>
        <td><%= user.email %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<p class="link"><%= link_to "Add New User", new_user_path %></p>
<p class="link"><%= link_to "Book Now", new_booking_path %></p>

